server:
  port: 5010
spring:
  application:
    name: poll-manager
  datasource:
    url: jdbc:postgresql://pollmanager.chyc7xrvj4x1.us-east-2.rds.amazonaws.com:5432/postgres
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
